namespace nilnul._time
{
	/// <summary>
	/// date is the periodical timespan that the earth rotates itself with respect to the sun;
	/// So each day might be of different length of clocks; We need to reset the clock each day, and this means timeline must be notated in data and clock format;
	/// </summary>
	/// alias:
	///		dateonly
	public interface IDate { }

}
